/** Compatibility facade; the transcript React component is owned by web-units. */
export {
  Transcript,
  type TranscriptDependencies,
  type TranscriptHandle,
  type TranscriptProps,
  type TranscriptRenderer,
} from '@agnes/web-units'
