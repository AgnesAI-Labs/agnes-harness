/** Compatibility facade for conservative error-message secret detection. */
export {
  domainFailureFromUnknown,
  isPublicErrorReason,
  looksLikeSecret,
  REDACTED_ERROR_MESSAGE,
} from '@agnes/error-sanitization'
