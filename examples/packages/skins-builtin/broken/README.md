# skins-builtin broken (test-only)

与其他家族的 `broken` 变体同形：**清单本身合法**，破损在源码树内容。
目录装载器会对每个家族检查 `agnes.extension.json`，清单不合法会被它直接判为目录不可用——
所以"broken"不能通过清单表达，只能通过树内容表达。

这个变体**只出现在测试专用的目录列表里**，正常发现目录不含它。
